DEBUG=True
SECRET_KEY=change-me-in-production

# Local Docker Postgres (see repo docker-compose.yml):
DATABASE_URL=postgresql://vaultclub:vaultclub@localhost:5432/vaultclub

# Render Postgres (use the External Database URL from the Render dashboard).
# If the URL does not include ?sslmode=require, set DATABASE_SSLMODE=require
# or rely on auto-detection for *.render.com hosts.
# DATABASE_URL=postgresql://USER:PASSWORD@HOST.region.render.com/DATABASE?sslmode=require

ALLOWED_HOSTS=localhost,127.0.0.1
CORS_ALLOWED_ORIGINS=http://localhost:3000

STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PUBLISHABLE_KEY=

FRONTEND_URL=http://localhost:3000
